#include "main.h"
/**
 * _puts_recursion - function that prints a string
 *
 * @s: pointer to strings
 *
 * Return: void
 */
void _puts_recursion(char *s);
	char s[100];

	gets(s);

	puts(s);
	if (*t == '\0')
	puts("%c", *s);
	puts(++t);
	_putchar('\n');
	return (0);
